import { IssueTitlePipe } from './issue-title.pipe';

describe('IssueTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new IssueTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
